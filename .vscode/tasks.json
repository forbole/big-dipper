{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start EXPLORER locally",
            "command": "meteor reset --allow-superuser && meteor --settings default_settings.json --allow-superuser",
            "type": "shell",
            "group": "build"
        }
    ]
}